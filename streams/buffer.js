const buf = Buffer.from("Hello"); // Buffer é um tipo de dado que representa um fluxo de dados de leitura e escrita.
// Buffer.from() é usado para criar um buffer a partir de uma string.

console.log(buf);
